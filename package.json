{
  "name": "gh-input-parser",
  "version": "1.0.0",
  "description": "A GitHub Action that parses the input and outputs it to matrix runner",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build index.js -o dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@vercel/ncc": "^0.38.2"
  }
}
