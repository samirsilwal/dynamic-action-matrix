name: Parse String
on:
  workflow_dispatch:
    inputs:
      value:
        description: 'From branch'
        required: true

jobs:
  parse-input:
    runs-on: ubuntu-latest
    name: Parsing input value
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v2
        with:
          node-version: 20

      - name: Parse inputs
        id: parse
        uses: ./
        with:
          value: ${{ github.event.inputs.value }}
      
      - name: Print parsed value
        run: echo "Parsed value is ${{ steps.parse.outputs.parsed }}"
    outputs:
      parsed: ${{ steps.parse.outputs.parsed }}

  matrix-example:
    needs: parse-input
    runs-on: ubuntu-latest
    strategy:
      matrix:
        input: ${{ needs.parse-input.outputs.parsed }}
    name: Matrix example

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v2
        with:
          node-version: 20

      - name: Print value
        run: echo "Value is ${{ matrix.input }}"
        
